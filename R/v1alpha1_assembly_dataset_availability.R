# NCBI Datasets API
#
# NCBI service to query and download biological sequence data across all domains of life from NCBI databases.
#
# The version of the OpenAPI document: v1alpha
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title V1alpha1AssemblyDatasetAvailability
#' @description V1alpha1AssemblyDatasetAvailability Class
#' @format An \code{R6Class} generator object
#' @field invalid_assemblies  list( character ) [optional]
#'
#' @field reason  character [optional]
#'
#' @field valid_assemblies  list( character ) [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
V1alpha1AssemblyDatasetAvailability <- R6::R6Class(
  'V1alpha1AssemblyDatasetAvailability',
  public = list(
    `invalid_assemblies` = NULL,
    `reason` = NULL,
    `valid_assemblies` = NULL,
    initialize = function(`invalid_assemblies`=NULL, `reason`=NULL, `valid_assemblies`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`invalid_assemblies`)) {
        stopifnot(is.vector(`invalid_assemblies`), length(`invalid_assemblies`) != 0)
        sapply(`invalid_assemblies`, function(x) stopifnot(is.character(x)))
        self$`invalid_assemblies` <- `invalid_assemblies`
      }
      if (!is.null(`reason`)) {
        stopifnot(is.character(`reason`), length(`reason`) == 1)
        self$`reason` <- `reason`
      }
      if (!is.null(`valid_assemblies`)) {
        stopifnot(is.vector(`valid_assemblies`), length(`valid_assemblies`) != 0)
        sapply(`valid_assemblies`, function(x) stopifnot(is.character(x)))
        self$`valid_assemblies` <- `valid_assemblies`
      }
    },
    toJSON = function() {
      V1alpha1AssemblyDatasetAvailabilityObject <- list()
      if (!is.null(self$`invalid_assemblies`)) {
        V1alpha1AssemblyDatasetAvailabilityObject[['invalid_assemblies']] <-
          self$`invalid_assemblies`
      }
      if (!is.null(self$`reason`)) {
        V1alpha1AssemblyDatasetAvailabilityObject[['reason']] <-
          self$`reason`
      }
      if (!is.null(self$`valid_assemblies`)) {
        V1alpha1AssemblyDatasetAvailabilityObject[['valid_assemblies']] <-
          self$`valid_assemblies`
      }

      V1alpha1AssemblyDatasetAvailabilityObject
    },
    fromJSON = function(V1alpha1AssemblyDatasetAvailabilityJson) {
      V1alpha1AssemblyDatasetAvailabilityObject <- jsonlite::fromJSON(V1alpha1AssemblyDatasetAvailabilityJson)
      if (!is.null(V1alpha1AssemblyDatasetAvailabilityObject$`invalid_assemblies`)) {
        self$`invalid_assemblies` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyDatasetAvailabilityObject$`invalid_assemblies`, "array[character]", loadNamespace("ncbi.datasets"))
      }
      if (!is.null(V1alpha1AssemblyDatasetAvailabilityObject$`reason`)) {
        self$`reason` <- V1alpha1AssemblyDatasetAvailabilityObject$`reason`
      }
      if (!is.null(V1alpha1AssemblyDatasetAvailabilityObject$`valid_assemblies`)) {
        self$`valid_assemblies` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyDatasetAvailabilityObject$`valid_assemblies`, "array[character]", loadNamespace("ncbi.datasets"))
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`invalid_assemblies`)) {
        sprintf(
        '"invalid_assemblies":
           [%s]
        ',
        paste(unlist(lapply(self$`invalid_assemblies`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`reason`)) {
        sprintf(
        '"reason":
          "%s"
                ',
        self$`reason`
        )},
        if (!is.null(self$`valid_assemblies`)) {
        sprintf(
        '"valid_assemblies":
           [%s]
        ',
        paste(unlist(lapply(self$`valid_assemblies`, function(x) paste0('"', x, '"'))), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(V1alpha1AssemblyDatasetAvailabilityJson) {
      V1alpha1AssemblyDatasetAvailabilityObject <- jsonlite::fromJSON(V1alpha1AssemblyDatasetAvailabilityJson)
      self$`invalid_assemblies` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyDatasetAvailabilityObject$`invalid_assemblies`, "array[character]", loadNamespace("ncbi.datasets"))
      self$`reason` <- V1alpha1AssemblyDatasetAvailabilityObject$`reason`
      self$`valid_assemblies` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyDatasetAvailabilityObject$`valid_assemblies`, "array[character]", loadNamespace("ncbi.datasets"))
      self
    }
  )
)
