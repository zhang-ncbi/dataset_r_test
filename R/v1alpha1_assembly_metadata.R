# NCBI Datasets API
#
# NCBI service to query and download biological sequence data across all domains of life from NCBI databases.
#
# The version of the OpenAPI document: v1alpha
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title V1alpha1AssemblyMetadata
#' @description V1alpha1AssemblyMetadata Class
#' @format An \code{R6Class} generator object
#' @field assemblies  list( \link{V1alpha1AssemblyMatch} ) [optional]
#'
#' @field messages  list( \link{V1alpha1Message} ) [optional]
#'
#' @field total_count  integer [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
V1alpha1AssemblyMetadata <- R6::R6Class(
  'V1alpha1AssemblyMetadata',
  public = list(
    `assemblies` = NULL,
    `messages` = NULL,
    `total_count` = NULL,
    initialize = function(`assemblies`=NULL, `messages`=NULL, `total_count`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`assemblies`)) {
        stopifnot(is.vector(`assemblies`), length(`assemblies`) != 0)
        sapply(`assemblies`, function(x) stopifnot(R6::is.R6(x)))
        self$`assemblies` <- `assemblies`
      }
      if (!is.null(`messages`)) {
        stopifnot(is.vector(`messages`), length(`messages`) != 0)
        sapply(`messages`, function(x) stopifnot(R6::is.R6(x)))
        self$`messages` <- `messages`
      }
      if (!is.null(`total_count`)) {
        stopifnot(is.numeric(`total_count`), length(`total_count`) == 1)
        self$`total_count` <- `total_count`
      }
    },
    toJSON = function() {
      V1alpha1AssemblyMetadataObject <- list()
      if (!is.null(self$`assemblies`)) {
        V1alpha1AssemblyMetadataObject[['assemblies']] <-
          lapply(self$`assemblies`, function(x) x$toJSON())
      }
      if (!is.null(self$`messages`)) {
        V1alpha1AssemblyMetadataObject[['messages']] <-
          lapply(self$`messages`, function(x) x$toJSON())
      }
      if (!is.null(self$`total_count`)) {
        V1alpha1AssemblyMetadataObject[['total_count']] <-
          self$`total_count`
      }

      V1alpha1AssemblyMetadataObject
    },
    fromJSON = function(V1alpha1AssemblyMetadataJson) {
      V1alpha1AssemblyMetadataObject <- jsonlite::fromJSON(V1alpha1AssemblyMetadataJson)
      if (!is.null(V1alpha1AssemblyMetadataObject$`assemblies`)) {
        self$`assemblies` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyMetadataObject$`assemblies`, "array[V1alpha1AssemblyMatch]", loadNamespace("ncbi.datasets"))
      }
      if (!is.null(V1alpha1AssemblyMetadataObject$`messages`)) {
        self$`messages` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyMetadataObject$`messages`, "array[V1alpha1Message]", loadNamespace("ncbi.datasets"))
      }
      if (!is.null(V1alpha1AssemblyMetadataObject$`total_count`)) {
        self$`total_count` <- V1alpha1AssemblyMetadataObject$`total_count`
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`assemblies`)) {
        sprintf(
        '"assemblies":
        [%s]
',
        paste(sapply(self$`assemblies`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`messages`)) {
        sprintf(
        '"messages":
        [%s]
',
        paste(sapply(self$`messages`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`total_count`)) {
        sprintf(
        '"total_count":
          %d
                ',
        self$`total_count`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(V1alpha1AssemblyMetadataJson) {
      V1alpha1AssemblyMetadataObject <- jsonlite::fromJSON(V1alpha1AssemblyMetadataJson)
      self$`assemblies` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyMetadataObject$`assemblies`, "array[V1alpha1AssemblyMatch]", loadNamespace("ncbi.datasets"))
      self$`messages` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyMetadataObject$`messages`, "array[V1alpha1Message]", loadNamespace("ncbi.datasets"))
      self$`total_count` <- V1alpha1AssemblyMetadataObject$`total_count`
      self
    }
  )
)
