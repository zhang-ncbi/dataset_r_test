# NCBI Datasets API
#
# NCBI service to query and download biological sequence data across all domains of life from NCBI databases.
#
# The version of the OpenAPI document: v1alpha
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title V1alpha1AssemblyMatch
#' @description V1alpha1AssemblyMatch Class
#' @format An \code{R6Class} generator object
#' @field assembly  \link{V1alpha1AssemblyDatasetDescriptor} [optional]
#'
#' @field messages  list( \link{V1alpha1Message} ) [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
V1alpha1AssemblyMatch <- R6::R6Class(
  'V1alpha1AssemblyMatch',
  public = list(
    `assembly` = NULL,
    `messages` = NULL,
    initialize = function(`assembly`=NULL, `messages`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`assembly`)) {
        stopifnot(R6::is.R6(`assembly`))
        self$`assembly` <- `assembly`
      }
      if (!is.null(`messages`)) {
        stopifnot(is.vector(`messages`), length(`messages`) != 0)
        sapply(`messages`, function(x) stopifnot(R6::is.R6(x)))
        self$`messages` <- `messages`
      }
    },
    toJSON = function() {
      V1alpha1AssemblyMatchObject <- list()
      if (!is.null(self$`assembly`)) {
        V1alpha1AssemblyMatchObject[['assembly']] <-
          self$`assembly`$toJSON()
      }
      if (!is.null(self$`messages`)) {
        V1alpha1AssemblyMatchObject[['messages']] <-
          lapply(self$`messages`, function(x) x$toJSON())
      }

      V1alpha1AssemblyMatchObject
    },
    fromJSON = function(V1alpha1AssemblyMatchJson) {
      V1alpha1AssemblyMatchObject <- jsonlite::fromJSON(V1alpha1AssemblyMatchJson)
      if (!is.null(V1alpha1AssemblyMatchObject$`assembly`)) {
        assemblyObject <- V1alpha1AssemblyDatasetDescriptor$new()
        assemblyObject$fromJSON(jsonlite::toJSON(V1alpha1AssemblyMatchObject$assembly, auto_unbox = TRUE, digits = NA))
        self$`assembly` <- assemblyObject
      }
      if (!is.null(V1alpha1AssemblyMatchObject$`messages`)) {
        self$`messages` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyMatchObject$`messages`, "array[V1alpha1Message]", loadNamespace("ncbi.datasets"))
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`assembly`)) {
        sprintf(
        '"assembly":
        %s
        ',
        jsonlite::toJSON(self$`assembly`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`messages`)) {
        sprintf(
        '"messages":
        [%s]
',
        paste(sapply(self$`messages`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(V1alpha1AssemblyMatchJson) {
      V1alpha1AssemblyMatchObject <- jsonlite::fromJSON(V1alpha1AssemblyMatchJson)
      self$`assembly` <- V1alpha1AssemblyDatasetDescriptor$new()$fromJSON(jsonlite::toJSON(V1alpha1AssemblyMatchObject$assembly, auto_unbox = TRUE, digits = NA))
      self$`messages` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyMatchObject$`messages`, "array[V1alpha1Message]", loadNamespace("ncbi.datasets"))
      self
    }
  )
)
