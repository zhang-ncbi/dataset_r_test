# NCBI Datasets API
#
# NCBI service to query and download biological sequence data across all domains of life from NCBI databases.
#
# The version of the OpenAPI document: v1alpha
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RuntimeStreamError
#' @description RuntimeStreamError Class
#' @format An \code{R6Class} generator object
#' @field details  list( \link{ProtobufAny} ) [optional]
#'
#' @field grpc_code  integer [optional]
#'
#' @field http_code  integer [optional]
#'
#' @field http_status  character [optional]
#'
#' @field message  character [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RuntimeStreamError <- R6::R6Class(
  'RuntimeStreamError',
  public = list(
    `details` = NULL,
    `grpc_code` = NULL,
    `http_code` = NULL,
    `http_status` = NULL,
    `message` = NULL,
    initialize = function(`details`=NULL, `grpc_code`=NULL, `http_code`=NULL, `http_status`=NULL, `message`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`details`)) {
        stopifnot(is.vector(`details`), length(`details`) != 0)
        sapply(`details`, function(x) stopifnot(R6::is.R6(x)))
        self$`details` <- `details`
      }
      if (!is.null(`grpc_code`)) {
        stopifnot(is.numeric(`grpc_code`), length(`grpc_code`) == 1)
        self$`grpc_code` <- `grpc_code`
      }
      if (!is.null(`http_code`)) {
        stopifnot(is.numeric(`http_code`), length(`http_code`) == 1)
        self$`http_code` <- `http_code`
      }
      if (!is.null(`http_status`)) {
        stopifnot(is.character(`http_status`), length(`http_status`) == 1)
        self$`http_status` <- `http_status`
      }
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
        self$`message` <- `message`
      }
    },
    toJSON = function() {
      RuntimeStreamErrorObject <- list()
      if (!is.null(self$`details`)) {
        RuntimeStreamErrorObject[['details']] <-
          lapply(self$`details`, function(x) x$toJSON())
      }
      if (!is.null(self$`grpc_code`)) {
        RuntimeStreamErrorObject[['grpc_code']] <-
          self$`grpc_code`
      }
      if (!is.null(self$`http_code`)) {
        RuntimeStreamErrorObject[['http_code']] <-
          self$`http_code`
      }
      if (!is.null(self$`http_status`)) {
        RuntimeStreamErrorObject[['http_status']] <-
          self$`http_status`
      }
      if (!is.null(self$`message`)) {
        RuntimeStreamErrorObject[['message']] <-
          self$`message`
      }

      RuntimeStreamErrorObject
    },
    fromJSON = function(RuntimeStreamErrorJson) {
      RuntimeStreamErrorObject <- jsonlite::fromJSON(RuntimeStreamErrorJson)
      if (!is.null(RuntimeStreamErrorObject$`details`)) {
        self$`details` <- ApiClient$new()$deserializeObj(RuntimeStreamErrorObject$`details`, "array[ProtobufAny]", loadNamespace("ncbi.datasets"))
      }
      if (!is.null(RuntimeStreamErrorObject$`grpc_code`)) {
        self$`grpc_code` <- RuntimeStreamErrorObject$`grpc_code`
      }
      if (!is.null(RuntimeStreamErrorObject$`http_code`)) {
        self$`http_code` <- RuntimeStreamErrorObject$`http_code`
      }
      if (!is.null(RuntimeStreamErrorObject$`http_status`)) {
        self$`http_status` <- RuntimeStreamErrorObject$`http_status`
      }
      if (!is.null(RuntimeStreamErrorObject$`message`)) {
        self$`message` <- RuntimeStreamErrorObject$`message`
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`details`)) {
        sprintf(
        '"details":
        [%s]
',
        paste(sapply(self$`details`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`grpc_code`)) {
        sprintf(
        '"grpc_code":
          %d
                ',
        self$`grpc_code`
        )},
        if (!is.null(self$`http_code`)) {
        sprintf(
        '"http_code":
          %d
                ',
        self$`http_code`
        )},
        if (!is.null(self$`http_status`)) {
        sprintf(
        '"http_status":
          "%s"
                ',
        self$`http_status`
        )},
        if (!is.null(self$`message`)) {
        sprintf(
        '"message":
          "%s"
                ',
        self$`message`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(RuntimeStreamErrorJson) {
      RuntimeStreamErrorObject <- jsonlite::fromJSON(RuntimeStreamErrorJson)
      self$`details` <- ApiClient$new()$deserializeObj(RuntimeStreamErrorObject$`details`, "array[ProtobufAny]", loadNamespace("ncbi.datasets"))
      self$`grpc_code` <- RuntimeStreamErrorObject$`grpc_code`
      self$`http_code` <- RuntimeStreamErrorObject$`http_code`
      self$`http_status` <- RuntimeStreamErrorObject$`http_status`
      self$`message` <- RuntimeStreamErrorObject$`message`
      self
    }
  )
)
