# NCBI Datasets API
#
# NCBI service to query and download biological sequence data across all domains of life from NCBI databases.
#
# The version of the OpenAPI document: v1alpha
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title V1alpha1AssemblyDatasetDescriptors
#' @description V1alpha1AssemblyDatasetDescriptors Class
#' @format An \code{R6Class} generator object
#' @field datasets  list( \link{V1alpha1AssemblyDatasetDescriptor} ) [optional]
#'
#' @field total_count  integer [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
V1alpha1AssemblyDatasetDescriptors <- R6::R6Class(
  'V1alpha1AssemblyDatasetDescriptors',
  public = list(
    `datasets` = NULL,
    `total_count` = NULL,
    initialize = function(`datasets`=NULL, `total_count`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`datasets`)) {
        stopifnot(is.vector(`datasets`), length(`datasets`) != 0)
        sapply(`datasets`, function(x) stopifnot(R6::is.R6(x)))
        self$`datasets` <- `datasets`
      }
      if (!is.null(`total_count`)) {
        stopifnot(is.numeric(`total_count`), length(`total_count`) == 1)
        self$`total_count` <- `total_count`
      }
    },
    toJSON = function() {
      V1alpha1AssemblyDatasetDescriptorsObject <- list()
      if (!is.null(self$`datasets`)) {
        V1alpha1AssemblyDatasetDescriptorsObject[['datasets']] <-
          lapply(self$`datasets`, function(x) x$toJSON())
      }
      if (!is.null(self$`total_count`)) {
        V1alpha1AssemblyDatasetDescriptorsObject[['total_count']] <-
          self$`total_count`
      }

      V1alpha1AssemblyDatasetDescriptorsObject
    },
    fromJSON = function(V1alpha1AssemblyDatasetDescriptorsJson) {
      V1alpha1AssemblyDatasetDescriptorsObject <- jsonlite::fromJSON(V1alpha1AssemblyDatasetDescriptorsJson)
      if (!is.null(V1alpha1AssemblyDatasetDescriptorsObject$`datasets`)) {
        self$`datasets` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyDatasetDescriptorsObject$`datasets`, "array[V1alpha1AssemblyDatasetDescriptor]", loadNamespace("ncbi.datasets"))
      }
      if (!is.null(V1alpha1AssemblyDatasetDescriptorsObject$`total_count`)) {
        self$`total_count` <- V1alpha1AssemblyDatasetDescriptorsObject$`total_count`
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasets`)) {
        sprintf(
        '"datasets":
        [%s]
',
        paste(sapply(self$`datasets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`total_count`)) {
        sprintf(
        '"total_count":
          %d
                ',
        self$`total_count`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(V1alpha1AssemblyDatasetDescriptorsJson) {
      V1alpha1AssemblyDatasetDescriptorsObject <- jsonlite::fromJSON(V1alpha1AssemblyDatasetDescriptorsJson)
      self$`datasets` <- ApiClient$new()$deserializeObj(V1alpha1AssemblyDatasetDescriptorsObject$`datasets`, "array[V1alpha1AssemblyDatasetDescriptor]", loadNamespace("ncbi.datasets"))
      self$`total_count` <- V1alpha1AssemblyDatasetDescriptorsObject$`total_count`
      self
    }
  )
)
