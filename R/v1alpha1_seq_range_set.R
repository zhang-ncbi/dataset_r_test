# NCBI Datasets API
#
# NCBI service to query and download biological sequence data across all domains of life from NCBI databases.
#
# The version of the OpenAPI document: v1alpha
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title V1alpha1SeqRangeSet
#' @description V1alpha1SeqRangeSet Class
#' @format An \code{R6Class} generator object
#' @field accession_version NCBI Accession.version of the sequence character [optional]
#'
#' @field range Series of intervals on above accession_version list( \link{V1alpha1Range} ) [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
V1alpha1SeqRangeSet <- R6::R6Class(
  'V1alpha1SeqRangeSet',
  public = list(
    `accession_version` = NULL,
    `range` = NULL,
    initialize = function(`accession_version`=NULL, `range`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`accession_version`)) {
        stopifnot(is.character(`accession_version`), length(`accession_version`) == 1)
        self$`accession_version` <- `accession_version`
      }
      if (!is.null(`range`)) {
        stopifnot(is.vector(`range`), length(`range`) != 0)
        sapply(`range`, function(x) stopifnot(R6::is.R6(x)))
        self$`range` <- `range`
      }
    },
    toJSON = function() {
      V1alpha1SeqRangeSetObject <- list()
      if (!is.null(self$`accession_version`)) {
        V1alpha1SeqRangeSetObject[['accession_version']] <-
          self$`accession_version`
      }
      if (!is.null(self$`range`)) {
        V1alpha1SeqRangeSetObject[['range']] <-
          lapply(self$`range`, function(x) x$toJSON())
      }

      V1alpha1SeqRangeSetObject
    },
    fromJSON = function(V1alpha1SeqRangeSetJson) {
      V1alpha1SeqRangeSetObject <- jsonlite::fromJSON(V1alpha1SeqRangeSetJson)
      if (!is.null(V1alpha1SeqRangeSetObject$`accession_version`)) {
        self$`accession_version` <- V1alpha1SeqRangeSetObject$`accession_version`
      }
      if (!is.null(V1alpha1SeqRangeSetObject$`range`)) {
        self$`range` <- ApiClient$new()$deserializeObj(V1alpha1SeqRangeSetObject$`range`, "array[V1alpha1Range]", loadNamespace("ncbi.datasets"))
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`accession_version`)) {
        sprintf(
        '"accession_version":
          "%s"
                ',
        self$`accession_version`
        )},
        if (!is.null(self$`range`)) {
        sprintf(
        '"range":
        [%s]
',
        paste(sapply(self$`range`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(V1alpha1SeqRangeSetJson) {
      V1alpha1SeqRangeSetObject <- jsonlite::fromJSON(V1alpha1SeqRangeSetJson)
      self$`accession_version` <- V1alpha1SeqRangeSetObject$`accession_version`
      self$`range` <- ApiClient$new()$deserializeObj(V1alpha1SeqRangeSetObject$`range`, "array[V1alpha1Range]", loadNamespace("ncbi.datasets"))
      self
    }
  )
)
